name: GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "examples/web/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions: read-all

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build web demo
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      present: ${{ steps.web.outputs.present }}
    steps:
      - uses: actions/checkout@v7
      - id: web
        run: |
          if [ -f examples/web/package-lock.json ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi
      - if: steps.web.outputs.present == 'true'
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: examples/web/package-lock.json
      - if: steps.web.outputs.present == 'true'
        working-directory: examples/web
        run: npm ci
      - if: steps.web.outputs.present == 'true'
        name: Build static site (no analytics)
        working-directory: examples/web
        run: npm run build
        env:
          VITE_BASE_PATH: /${{ github.event.repository.name }}/
      - if: steps.web.outputs.present == 'true'
        uses: actions/upload-pages-artifact@v5
        with:
          path: examples/web/dist

  deploy:
    name: Deploy to GitHub Pages
    if: needs.build.outputs.present == 'true'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    # github-pages is the only allowed Environment. Do not copy this onto CI,
    # Security Scan, or CodeQL — those required checks must stay unblocked.
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
