# Sprint 0 super workflow

Read and execute each sub-command in order. After each step, summarize pass/fail before continuing.

1. Read @.cursor/commands/init.md — execute fully
2. Read @.cursor/commands/prune.md — execute fully (skip if stack is `none` / all examples kept)
3. Read @.cursor/commands/setup.md — execute fully
4. Read @.cursor/commands/gates.md — execute fully
5. **Welcome Tour** (after gates): tell the human, briefly:
   - Recommended read order and why (`docs/START_HERE.md` → `docs/CURSOR_MODES.md` → `docs/BEST_PRACTICES.md` → `AGENTS.md` → BUILD_PLAN Sequential)
   - Every task has an AGENT/HUMAN/ADB/AUTO label so agents do not block on credentials or devices
   - `AGENT_MEMORY.md` is long-term brain — update it only at milestones to avoid token waste
   - Point to `docs/BEST_PRACTICES.md` and `docs/FIRST_30_DAYS.md`; offer to walk the first 3–4 items
   - Mention `/coach` for later sessions

Begin now.
